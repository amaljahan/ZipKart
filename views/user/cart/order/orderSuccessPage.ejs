<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Success</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@3.0.0/dist/tailwind.min.css" rel="stylesheet">
    <style>
        /* Body styles */
        body {
            background: #2874f0; /* Primary website color */
            color: white;
            font-family: 'Poppins', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            overflow: hidden; /* Prevent scrolling */
        }

        /* Success container */
        .success-container {
            background: white;
            color: #2874f0;
            text-align: center;
            padding: 40px 30px;
            border-radius: 12px;
            box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.2);
            max-width: 400px;
            width: 90%;
            opacity: 0; /* Hidden initially */
            transform: translateY(50px); /* Slide effect */
            animation: fadeIn 0.6s ease-out forwards; /* Fade-in with slide */
        }

        /* Checkmark icon animation */
        .success-icon {
            font-size: 50px;
            margin-bottom: 15px;
            color: #4CAF50;
            animation: bounce 1s ease infinite; /* Bouncing effect */
        }

        /* Title and text */
        .success-title {
            font-size: 1.8rem;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .success-text {
            font-size: 1rem;
            margin-bottom: 20px;
            line-height: 1.5;
        }

        /* Buttons */
        .btn {
            display: inline-block;
            background: #2874f0;
            color: white;
            text-transform: uppercase;
            padding: 10px 20px;
            border-radius: 8px;
            font-size: 0.9rem;
            font-weight: bold;
            margin: 10px 5px;
            text-decoration: none;
            transition: background-color 0.3s ease, transform 0.2s;
        }

        .btn:hover {
            background: #0056b3;
            transform: translateY(-3px); /* Button hover lift effect */
        }

        .btn-outline {
            background: white;
            color: #2874f0;
            border: 2px solid #2874f0;
        }

        .btn-outline:hover {
            background: #2874f0;
            color: white;
            transform: translateY(-3px); /* Button hover lift effect */
        }

        /* Keyframes for animations */
        @keyframes fadeIn {
            0% {
                opacity: 0;
                transform: translateY(50px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes bounce {
            0%, 100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-10px);
            }
        }
    </style>
</head>
<body>
    <div class="success-container">
        <!-- Icon -->
        <div class="success-icon">✔</div>

        <!-- Title -->
        <h1 class="success-title">Order Placed!</h1>

        <!-- Message -->
        <p class="success-text">
            Thank you for shopping with us!<br>
            Your order ID is: <strong><%= orderId || "Order Id" %></strong>.
        </p>

        <!-- Buttons -->
        <a href="/zipkart/user/accountDetails/my-orders" class="btn">View Orders</a>
        <a href="/zipkart/user/home" class="btn btn-outline">Continue Shopping</a>
    </div>
</body>
</html>
